# Microfrontend Demonstration Core Application

Run `ng serve` to start the dev server. Navigate to `http://localhost:4200/`.

Each capability application must be started separately on their own ports.
